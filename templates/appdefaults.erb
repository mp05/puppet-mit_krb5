<% fields = [
    'application',
    'app_realm',
    'app_options',
]
output = []
scope_hash = scope.to_hash
value = scope_hash['app_options']
if value.respond_to?('each')
  value.each do |option, opt_value|
    output.push("#{option} = #{opt_value}")
  end
end
-%>
<% if scope_hash['application'] == true -%>
  <%= @title %> = {
<% end -%>
<% if not scope_hash['app_realm'].empty? -%>
    <%= @app_realm %> = {
<% end -%>
        <%= output.join("\n        ") %>
<% if scope_hash['application'] == true -%>
    }
<% end -%>
<% if not scope_hash['app_realm'].empty? -%>
  }
<% end -%>
